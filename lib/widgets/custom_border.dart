import 'package:flutter/material.dart';
import 'custom_color.dart';

var borderSide =
    const BorderSide(color: black, width: 1.0, style: BorderStyle.solid);
final BorderRadius borderRadius = BorderRadius.circular(20);
